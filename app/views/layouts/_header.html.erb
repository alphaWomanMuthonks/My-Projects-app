<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <% if user_signed_in? %> 
     <h2 class="navbar-brand" > Hello <%= current_user.name %> </h2>
    <% else %>
      <h2 class="navbar-brand"> Please, Sign Up <h2>
    <% end %>   

    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if user_signed_in? %>
        
             <li class="nav-item">
               <%= link_to "Projects", projects_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
               <%= link_to "Add Project", new_project_path, class: "nav-link" %>
            </li>
        <% else %>
           <li class="nav-item">
              <%= link_to "Sign up" , new_user_registration_path , class: "btn btn-outline-primary mx-4"%>
            </li>
            <li class="nav-item">
               <%= link_to "Sign in" , new_user_session_path , class: "btn btn-outline-primary"%>
            </li>
        <% end %>    


        
       </ul>
       <% !if user_signed_in? %>
      <form class="d-flex" role="search">
          <%= link_to "Sign Out", destroy_user_session_path , class: "btn btn-outline-primary" %>

      </form>
      <% end %>
      
    </div>
  </div>
</nav>